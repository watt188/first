# Chief of Staff

Production baseline repository for the Chief-of-Staff multi-agent engineering runtime.

Current baseline: V6.0

Core production surface: `run`, `verify`, `release`, `recover`, `status`.
