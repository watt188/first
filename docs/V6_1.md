# Chief-of-Staff V6.1 — Real Model Provider Integration

V6.1 adds a real OpenAI-compatible model-provider path to the hardened production baseline.

Production rules:
- require MODEL_API_KEY, MODEL_BASE_URL, MODEL_NAME;
- provider output must be valid JSON operations;
- every file write still passes through the V5.9 default-deny PEP;
- non-writer roles cannot gain write authority;
- fake provider is never selected automatically in production.

FakeProviderV61 exists only for deterministic CI acceptance.
